{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "wmake-build",
            "type": "shell",
            "command": "wmake && ${env:FOAM_USER_APPBIN}/${fileBasenameNoExtension} -case ../depthCharge2D",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}